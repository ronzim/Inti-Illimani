name: Reviewer
on:
    pull_request:
        types: [opened, synchronize]
permissions:
  contents: read
  pull-requests: write

jobs:
  call-central-workflow:
    uses: dvisionlab/central-reviewer/.github/workflows/gemini-review.yml@master
